#!/bin/sh

# a merge that applies cleanly commits without running pre-commit, which is where the
# checks live. a conflicted one is finished by hand with git commit, so it reaches them
# on its own
exec "$(dirname "$0")/pre-commit"
